# プロジェクト構造

## 📁 ディレクトリ構成

```
business-system/
├── 📁 client/                    # Next.js フロントエンド
│   ├── 📁 app/                   # Next.js App Router
│   │   ├── 📁 admin/             # 管理者ページ
│   │   ├── 📁 dashboard/         # ダッシュボードページ
│   │   ├── 📁 login/             # ログインページ
│   │   ├── layout.tsx            # ルートレイアウト
│   │   └── page.tsx              # ホームページ
│   ├── 📁 components/            # React コンポーネント
│   │   ├── 📁 admin/             # 管理者コンポーネント
│   │   ├── 📁 auth/              # 認証コンポーネント
│   │   ├── 📁 layout/            # レイアウトコンポーネント
│   │   ├── 📁 sheets/            # シートコンポーネント
│   │   ├── 📁 ui/                # shadcn/ui コンポーネント
│   │   └── 📁 user/              # ユーザーコンポーネント
│   ├── 📁 lib/                   # フロントエンドユーティリティ
│   │   ├── api.ts                # API呼び出しユーティリティ
│   │   └── utils.ts              # 共通ユーティリティ
│   ├── 📄 package.json           # クライアント依存関係
│   ├── 📄 next.config.js         # Next.js設定
│   ├── 📄 tailwind.config.js     # Tailwind設定
│   ├── 📄 tsconfig.json          # TypeScript設定
│   └── 📄 env.example            # 環境変数テンプレート
├── 📁 server/                    # Express バックエンド
│   ├── 📁 routes/                # API ルート
│   │   ├── auth.ts               # 認証API
│   │   ├── admin.ts              # 管理者API
│   │   ├── user.ts               # ユーザーAPI
│   │   ├── calculate.ts          # 計算API
│   │   └── pdf.ts                # PDF生成API
│   ├── 📁 middleware/            # ミドルウェア
│   │   └── auth.ts               # 認証ミドルウェア
│   ├── 📁 lib/                   # サーバーユーティリティ
│   │   └── calculation-engine.ts # 計算エンジン
│   ├── 📁 prisma/                # データベース
│   │   ├── schema.prisma         # データベーススキーマ
│   │   └── seed.ts               # シードデータ
│   ├── 📄 index.ts               # メインサーバーファイル
│   ├── 📄 package.json           # サーバー依存関係
│   ├── 📄 tsconfig.json          # TypeScript設定
│   └── 📄 env.example            # 環境変数テンプレート
├── 📄 package.json               # ルート依存関係・スクリプト
├── 📄 README.md                  # プロジェクトドキュメント
├── 📄 setup.sh                   # Linux/Mac セットアップスクリプト
├── 📄 setup.bat                  # Windows セットアップスクリプト
└── 📄 .gitignore                 # Git除外設定
```

## 🚀 起動方法

### 開発環境
```bash
# フロントエンドとバックエンドを同時起動
npm run dev

# 個別起動
npm run dev:server  # バックエンド (ポート3001)
npm run dev:client  # フロントエンド (ポート3000)
```

### 本番環境
```bash
# ビルド
npm run build

# 起動
npm run start:server  # バックエンド
npm run start:client  # フロントエンド
```

## 📦 依存関係

### ルート
- `concurrently` - 同時実行

### サーバー
- `express` - Webサーバー
- `@prisma/client` - データベースORM
- `bcryptjs` - パスワードハッシュ化
- `jsonwebtoken` - JWT認証
- `cors` - CORS設定
- `cookie-parser` - クッキー解析

### クライアント
- `next` - Reactフレームワーク
- `react` - UIライブラリ
- `tailwindcss` - CSSフレームワーク
- `@radix-ui/*` - UIコンポーネント
- `lucide-react` - アイコン
- `recharts` - グラフ

## 🔧 開発ツール

### データベース
```bash
npm run db:generate  # Prismaクライアント生成
npm run db:push      # スキーマ同期
npm run db:migrate   # マイグレーション
npm run db:studio    # Prisma Studio
npm run db:seed      # シードデータ作成
```

### その他
```bash
npm run lint         # リント実行
npm run clean        # クリーンアップ
```

## 🌐 アクセス

- **フロントエンド**: http://localhost:3000
- **バックエンドAPI**: http://localhost:3001
- **ヘルスチェック**: http://localhost:3001/api/health
